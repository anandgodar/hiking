---
// DIRECT IMPORT - No guessing paths
// We are importing the exact file you pasted content from
import myData from '../data/nh/mt-washington-nh.json';
---

<html>
  <body style="padding: 50px; font-family: sans-serif;">
    <h1>🧪 The Sanity Test</h1>

    <div style="background: #f0f0f0; padding: 20px; border-radius: 10px;">
      <h2>1. Can we read the name?</h2>
      <p style="font-size: 20px; color: blue;">
        {myData.name}
      </p>

      <h2>2. Can we read the description?</h2>
      {myData.generated_description ? (
        <div style="border: 2px solid green; padding: 10px;">
            <h3>YES! Here it is:</h3>
            <div set:html={myData.generated_description} />
        </div>
      ) : (
        <h3 style="color: red;">NO. The property is missing.</h3>
      )}
    </div>

    <h2>3. Raw Data Dump</h2>
    <pre>{JSON.stringify(myData, null, 2)}</pre>
  </body>
</html>